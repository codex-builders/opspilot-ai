You are preparing a post-incident report draft. Use only the supplied context.

Return valid JSON only with: `executive_summary`, `customer_impact`,
`timeline`, `root_cause_status`, `root_cause_hypothesis`, `mitigations`, and
`follow_up_actions`.

Keep root cause as a hypothesis unless confirmed in the source context.

Incident context:
`{normalized_incident_context}`
